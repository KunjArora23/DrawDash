const VideoOverlay = () => {
  return (
    <div className="absolute top-4 right-4 hidden md:grid grid-cols-2 gap-2">
      {/* Future video tiles (max 4 users) */}
    </div>
  );
};

export default VideoOverlay;
